all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
  vars:
    k8s_namespace: "locatic-infra"
    app_image: "locatic-app:main" 
    app_replicas: 2